dist: bionic

addons:
  apt:
    update: true
    packages:
      - liblcm-dev
      - python-liblcm

git:
  depth: 3

language: python
python:
  - '3.6'
  - '3.7'

# Travis installs requirements from pip by default
#install:
#  - pip install -r requirements.txt

script:
  - pushd test/types && lcm-gen -p *.lcm && popd
  - python3 lcmlog_export.py --lcmtypes test/types test/data/lcmlog-test.00
  - pytest
